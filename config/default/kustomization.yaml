# Adds namespace to all resources.
namespace: sonarqube-operator-system

# Value of this field is prepended to the
# names of all resources, e.g. a deployment named
# "wordpress" becomes "alices-wordpress".
# Note that it should also match with the prefix (text before '-') of the namespace
# field above.
namePrefix: sonarqube-operator-

# Labels to add to all resources and selectors.
#commonLabels:
#  someName: someValue

bases:
- ../crd
- ../manager
# Apply this rbac set if cluster scope is desired
- ../rbac/cluster_scope
# Apply this rbac set if namespace scope is desired
# - ../rbac/namespace_scope

# Only use this when using namespace scope
# patchesStrategicMerge:
# - namespace_scope_patch.yaml

# [PROMETHEUS] To enable prometheus monitor, uncomment all sections with 'PROMETHEUS'.
#- ../prometheus

# patchesStrategicMerge:
  # Protect the /metrics endpoint by putting it behind auth.
  # If you want your controller-manager to expose the /metrics
  # endpoint w/o any authn/z, please comment the following line.
# - manager_auth_proxy_patch.yaml
